<!-- Version B - Combined Hero/Video Section (Zillow Style) -->
<section class='hero-video-section'>
  <!-- Banner Header -->
  <div class='banner-header d-flex align-items-center'>
    <div class='container'>
      <div class='text-center py-4'>
        <h2 class='banner-title text-white text-uppercase mb-0'>
          Maximize Your Lands Value
        </h2>
      </div>
    </div>
  </div>

  <div class='container py-5'>
    <div class='row justify-content-between'>
      <!-- Left Column - Title and Video -->
      <div class='col-lg-6 mb-5 mb-lg-0'>
        <!-- Pill Button -->
        <div class='mb-3'>
          <a href='#' class='btn btn-outline-dark rounded-pill'>
            Whitetail Properties Overview
          </a>
        </div>

        <!-- Main Heading -->
        <h1 class='display-4 fw-bold mb-4'>
          Use Whitetail Properties to help win more listings in your market
        </h1>

        <!-- Subtitle Text -->
        <p class='lead mb-5'>
          Our specialized team connects buyers with exceptional properties and
          helps sellers showcase their land's unique features that discerning
          land enthusiasts appreciate.<sup>*</sup>
        </p>

        <!-- Video Player - Now using iframe directly instead of thumbnail with play button -->
        <div class='ratio ratio-16x9 mb-4 rounded overflow-hidden shadow'>
          <iframe
            src='https://www.youtube.com/embed/Ptukw3cxTEo'
            title='Why Choose Whitetail Properties?'
            allowfullscreen></iframe>
        </div>
      </div>

      <!-- Right Column - Form (Using the form from your second paste) -->
      <div class='col-lg-5'>
        <div class='form-container bg-white rounded shadow p-4'>
          <h2 class='h3 text-center mb-4'>Lets Get Started</h2>
          <form>
            <!-- Name Field -->
            <div class='mb-3'>
              <label for='name' class='form-label lead'>
                Name <span class='text-danger'>*</span>
              </label>
              <input
                type='text'
                class='form-control'
                id='name'
                placeholder='e.g. John Smith'
                required
              />
            </div>

            <!-- Email Field -->
            <div class='mb-3'>
              <label for='email' class='form-label lead'>
                Email <span class='text-danger'>*</span>
              </label>
              <input
                type='email'
                class='form-control'
                id='email'
                placeholder='john@doe.com'
                required
              />
            </div>

            <!-- Phone Field -->
            <div class='mb-3'>
              <label for='phone' class='form-label lead'>Phone</label>
              <input
                type='tel'
                class='form-control'
                id='phone'
                placeholder='XXX XXX-XXXX'
              />
            </div>

            <!-- Postal Code Field -->
            <div class='mb-3'>
              <label for='postalCode' class='form-label lead'>
                Postal Code <span class='text-danger'>*</span>
              </label>
              <input
                type='text'
                class='form-control'
                id='postalCode'
                placeholder='12345'
                required
              />
            </div>

            <!-- Help Selection Field -->
            <div class='mb-3'>
              <label for='helpType' class='form-label lead'>
                How can we help? <span class='text-danger'>*</span>
              </label>
              <select class='form-select' id='helpType' required>
                <option value='' selected disabled>Select an option</option>
                <option value='buy'>I want to buy land</option>
                <option value='sell'>I want to sell my land</option>
                <option value='info'>I need more information</option>
              </select>
            </div>

            <!-- Consent Checkbox -->
            <div class='mb-4 form-check'>
              <input type='checkbox' class='form-check-input' id='consent' />
              <label class='form-check-label' for='consent'>
                I agree to receive updates and information through email
              </label>
            </div>

            <!-- Submit Button -->
            <div class='d-grid'>
              <button type='submit' class='btn submit-btn text-white py-2'>
                SUBMIT INFO →
              </button>
            </div>

            <!-- Privacy Text -->
            <div class='text-center text-muted small mt-3'>
              We respect your privacy. See our
              <a href='#' class='text-decoration-none'>Privacy Policy</a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Minimal custom CSS */
  .banner-header {
    background-color: #2a2a2a;
    background-image: url("/images/land.png");
    background-size: cover;
    background-position: center;
    position: relative;
    height: 320px;
  }

  .banner-header::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.4);
  }

  .banner-title {
    position: relative;
    letter-spacing: 2px;
    font-weight: 600;
  }

  /* Form styling from your second paste */
  .form-control,
  .form-select {
    padding: 0.75rem 1rem;
    font-size: 1.1rem;
    border-radius: 0.5rem;
  }

  .form-label {
    font-size: 1.1rem;
    font-weight: 500;
    margin-bottom: 0.5rem;
  }

  .form-check-input {
    width: 1.25rem;
    height: 1.25rem;
  }

  .form-check-label {
    padding-left: 0.25rem;
    font-size: 0.95rem;
  }

  .submit-btn {
    padding: 0.875rem 1.5rem;
    font-size: 1.1rem;
    font-weight: 500;
    letter-spacing: 0.5px;
    background-color: #d4b978;
    border-color: #d4b978;
  }

  .submit-btn:hover,
  .submit-btn:focus {
    background-color: #c5aa69;
    border-color: #c5aa69;
  }

  @media (max-width: 991.98px) {
    .video-container {
      max-width: 100%;
      margin: 0 auto;
    }
  }
</style>
